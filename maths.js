// var p = 500;
// var r = 3;
// var t = 2;

// var i = (p * t * r) / 100;
// console.log(`The interest of ${p} at the rate of ${r}% for ${t} weeks is ${i}`);

var he = 15;
var ra = 4 ** 2;
var pie = 22 / 7;

var all = 2 * (pie * ra + 2) * (pie * ra * he);
console.log(all);
